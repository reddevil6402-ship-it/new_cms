server:
  port: 8082

spring:
  application:
    name: cms-schema-service

  datasource:
    url: jdbc:postgresql://localhost:5435/schemadb
    username: cms_schema
    password: localpassword
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: schema

  flyway:
    enabled: true
    schemas: schema
    create-schemas: true
    connect-retries: 10

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${JWKS_URI:http://localhost:8081/api/v1/auth/.well-known/jwks.json}

logging:
  level:
    root: INFO
    com.cms.schema: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
